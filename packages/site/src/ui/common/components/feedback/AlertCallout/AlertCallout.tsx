import { AlertCalloutRoot } from './AlertCallout.styles';
import type { AlertCalloutProps } from './AlertCallout.types';

function AlertCallout(props: AlertCalloutProps) {
  return <AlertCalloutRoot {...props} />;
}

export default AlertCallout;
